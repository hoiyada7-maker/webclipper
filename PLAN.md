# 웹 클리퍼 이미지 텍스트/그림 분리 파이프라인 구축 계획 (PLAN.md)

이미지에서 텍스트와 그림을 분리하여 마크다운 파일로 자동 변환하는 시스템을 구축하기 위한 계획입니다. 시스템은 빠른 구현을 위한 **프로토타입(Prototype)**과 실무 및 고도화 적용을 위한 **고급타입(Advanced Type)** 두 가지 방향으로 나누어 계획했습니다.

---

## 1. 프로토타입 (Prototype)

**목표:** 로컬 환경에서 오픈소스만을 사용하여 빠르고 비용 없이 기본 파이프라인의 동작 가능성을 검증합니다.

### 핵심 기술 선택 (Prototype)
*   **Layout 분석:** `LayoutParser` (PubLayNet 모델)
*   **OCR 엔진:** `Tesseract` (로컬 설치)
*   **이미지 처리:** `OpenCV`, `Pillow`

### 💡 선택에 대한 장단점
*   **장점 (Pros):**
    *   **비용 무료:** 클라우드 API 사용료가 전혀 발생하지 않습니다.
    *   **데이터 프라이버시:** 모든 처리가 로컬에서 이루어지므로 민감한 데이터 유출 위험이 없습니다.
    *   **빠른 시작:** 파이썬 라이브러리와 Tesseract 설치만으로 즉각적인 테스트가 가능합니다.
*   **단점 (Cons):**
    *   **한글 인식률 한계:** Tesseract는 배경이 복잡하거나 폰트가 특이한 경우 한글 인식률이 다소 떨어집니다.
    *   **환경 설정:** Tesseract OS 설치 경로 설정 및 LayoutParser 종속성(PyTorch 등) 설치가 다소 번거로울 수 있습니다.

### ✅ ToDo 리스트 (Prototype)
- [ ] **환경 세팅**
  - [ ] Tesseract-OCR 운영체제(Windows) 내 설치 및 환경 변수 등록
  - [ ] 파이썬 필수 라이브러리 설치 (`pip install layoutparser torchvision opencv-python pytesseract pillow`)
- [ ] **파이프라인 스크립트 작성**
  - [ ] 이미지 로드 및 `cv2`를 이용한 전처리
  - [ ] `LayoutParser` (Detectron2 기반) 모델 초기화 및 레이아웃(Text, Title, Figure 등) 감지 로직 구현
  - [ ] 감지된 블록을 Y축 기준으로 정렬 (위에서 아래 순서)
- [ ] **데이터 추출 및 변환**
  - [ ] Figure(그림) 영역 크롭(Crop) 및 로컬 이미지 파일 저장
  - [ ] Text/Title 영역 크롭 및 `pytesseract`를 이용한 문자 인식(OCR) 실행
- [ ] **마크다운(.md) 통합**
  - [ ] 추출된 텍스트와 저장된 그림의 로컬 경로를 조합하여 `.md` 파일 자동 생성 로직 완성
  - [ ] 샘플 이미지를 통한 테스트 및 디버깅

---

## 2. 고급타입 (Advanced Type)

**목표:** 높은 한글 인식률과 사용 편의성(자동화)을 확보하여 실제 업무나 노트 정리에 즉시 활용할 수 있는 수준으로 고도화합니다.

### 핵심 기술 선택 (Advanced Type)
*   **Layout 분석:** `PaddleOCR (PP-Structure)` 또는 클라우드 AI 문서 분석 API
*   **OCR 엔진:** 상용 클라우드 API (`Naver Clova OCR`, `Google Cloud Vision API`)
*   **자동화 도구:** 클립보드 감지 라이브러리 (`pyperclip`, `Pillow ImageGrab`)

### 💡 선택에 대한 장단점
*   **장점 (Pros):**
    *   **압도적인 인식률:** 상용 OCR 엔진을 사용하여 한글 텍스트 및 복잡한 표/문서 구조도 정확하게 추출합니다.
    *   **뛰어난 사용성:** 스크린샷 캡처 시 백그라운드에서 자동으로 마크다운이 생성되므로 워크플로우가 획기적으로 개선됩니다.
    *   **안정적인 구조 분석:** PP-Structure는 테이블(표) 분석 등 복잡한 문서 구조를 HTML이나 Markdown으로 쉽게 변환해 줍니다.
*   **단점 (Cons):**
    *   **비용 발생:** 클라우드 API 호출량에 따라 종량제 비용이 발생할 수 있습니다.
    *   **네트워크 의존성:** 인터넷이 연결되어 있어야 정상적으로 작동합니다.
    *   **키 관리:** API Key 발급 및 보안 관리가 필요합니다.

### ✅ ToDo 리스트 (Advanced Type)
- [ ] **고급 OCR 환경 구성**
  - [ ] Naver Clova OCR 또는 Google Cloud Vision API 계정 가입 및 API Key 발급
  - [ ] 발급된 API를 파이썬 스크립트와 연동하는 테스트 코드 작성
- [ ] **Layout 구조 분석 고도화**
  - [ ] `PaddleOCR` (PP-Structure) 설치 및 테스트 (복잡한 레이아웃 분석용)
  - [ ] 기존 `LayoutParser` 로직을 PP-Structure 또는 Cloud API 기반으로 교체
- [ ] **실시간 자동화 파이프라인 구축**
  - [ ] `pyperclip` 및 `Pillow`를 활용하여 OS 클립보드 이미지 변경 감지 리스너 구현
  - [ ] 새로운 이미지가 클립보드에 들어오면 자동으로 파이프라인이 백그라운드에서 실행되도록 연동
- [ ] **마크다운 서식 고급화**
  - [ ] 표(Table)가 인식될 경우 마크다운 표 문법으로 자동 변환 로직 추가
  - [ ] OCR 후처리 (정규식을 활용한 불필요한 개행 제거, 오탈자 보정 등) 적용
- [ ] **배포 및 패키징 (Optional)**
  - [ ] 파이썬 스크립트를 백그라운드 실행 파일(`.exe` 또는 서비스)로 패키징하여 시작 프로그램에 등록
